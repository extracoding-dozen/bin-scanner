# Go Signature Scanner (Trie-based Pattern Matcher)

![Go Version](https://img.shields.io/badge/Go-1.20+-00ADD8?style=flat&logo=go)
![License](https://img.shields.io/badge/License-MIT-blue.svg)

**High-performance byte stream pattern matcher.**

Этот проект представляет собой высокопроизводительный сканер сигнатур, написанный на **Go**. Он реализует движок поиска байтовых последовательностей в бинарных файлах с использованием структуры данных **Prefix Tree (Trie)**.

Проект создан для демонстрации работы с алгоритмами поиска, оптимизации работы с памятью и структурирования Go-приложений. Основное назначение — обнаружение вредоносного кода (сигнатурный анализ) или поиск "магических чисел" в потоках данных.

---

## 🚀 Ключевые особенности (Key Features)

*   **Trie Algorithm:** Эффективное хранение и поиск множества сигнатур. Сложность поиска зависит от длины входного файла и глубины дерева, а не от общего количества сигнатур в базе.
*   **Wildcard Support (`??`):** Поддержка масок. Сигнатура `AA ?? BB` найдет последовательности `AA 00 BB`, `AA FF BB` и т.д.
*   **Longest Match Logic:** При наличии пересекающихся сигнатур (например, `Signature` и `SignatureExtended`) сканер корректно определяет и возвращает наиболее длинное и точное совпадение.
*   **Zero-Allocation Optimization:** Для конвертации байтов в hex-строки используется предвычисленная таблица (`lookup table`), что исключает лишние аллокации памяти (GC pressure) во время сканирования.
*   **Clean Architecture:** Четкое разделение ответственности между пакетами (`scanner`, `prefix_tree`, `signature`).

---

## 🛠 Технический стек (Tech Stack)

*   **Language:** Golang
*   **Data Structures:** Prefix Tree (Trie), HashMap
*   **Optimization:** Look-up tables, DFS (Depth-First Search)
*   **I/O:** `os`, `bufio`

---

## 📂 Структура проекта (Project Structure)

```text
SignatureScanner/
├── internal/
│   ├── file_position/
│   │   └── Position.go          # Модель результата (Match)
│   ├── prefix_tree/
│   │   ├── Node.go              # Узел дерева
│   │   └── Tree.go              # Логика дерева (Вставка, Поиск)
│   └── signature/
│       ├── Signature.go         # Сущность сигнатуры
│       └── SignatureContainer.go # Контейнер (если используется)
├── src/
│   └── main.go                  # Точка входа (Запуск программы)
├── SignatureScanner.go          # Основной класс сканера (Lib)
├── go.mod                       # Зависимости
├── signatures.txt               # База сигнатур
└── malware.txt                  # Файл для теста
```
## ⚙️ Алгоритм работы

1.  **Инициализация:** При старте программа считывает файл с сигнатурами. Каждая HEX-строка разбивается на токены (байты или `??`) и вставляется в **Префиксное дерево**.
2.  **Сканирование:** Целевой файл считывается как массив байт.
3.  **Поиск:**
    *   Программа проходит по массиву байт.
    *   Для каждого смещения запускается рекурсивный поиск в глубину (DFS) по дереву.
    *   Благодаря `HexLookupTable`, преобразование байта в строку для сверки с узлом дерева происходит за **O(1)** без выделения памяти.
4.  **Результат:** Если путь в дереве приводит к узлу с флагом окончания сигнатуры, фиксируется смещение и имя сигнатуры.

---

## 🚀 Запуск и использование

### 1. Клонирование
```bash
git clone https://github.com/your-username/signature-scanner.git
cd SignatureScanner
```

  

### 2. Подготовка базы (`signatures.txt`)

Файл должен содержать HEX-последовательности (по одной на строку).
Пример:
```text
48454c4c4f         # HELLO (Точное совпадение)
cafebabe           # Java Magic Class (Магическое число)
4841434b??         # HACK + 1 любой байт (Wildcard)
```

### 3. Запуск сканера

Так как точка входа находится в папке src:
code Bash

```bash
go buils -o scanner ./src/
```
  

**Примечание:** Убедитесь, что в main.go указаны правильные имена файлов для базы сигнатур и сканируемого файла.
📊 Пример работы

Допустим, сканируется файл test.bin, содержащий байты `SYSTEM ERROR 0xCAFEBABE`.

Вывод консоли:
```text
>>> Загрузка базы сигнатур... OK.
>>> Сканирование файла test.bin...

[!!!] Найдено совпадение:
 -> Смещение: 7  (0x07) | Сигнатура: 4552524f52 (ERROR)
 -> Смещение: 15 (0x0F) | Сигнатура: cafebabe

Сканирование завершено. Угроз: 2
```

## 📈 Планы по улучшению (To Do)

1. Реализация алгоритма Ахо-Корасик для достижения линейной сложности поиска O(N).

2. Внедрение буферизированного чтения (Buffered IO) для обработки файлов большого объема (> RAM).

3. Добавление конкурентности (Goroutines) для параллельного сканирования множества файлов.

4. CLI-интерфейс с флагами (например, -f file.bin -s base.txt).